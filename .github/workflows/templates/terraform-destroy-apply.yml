name: Terraform Destroy Apply Template

on:
  workflow_call:
    inputs:
      terraform_version:
        description: 'Terraform version to use'
        required: false
        type: string
        default: '1.6.0'
      working_directory:
        description: 'Working directory for Terraform'
        required: false
        type: string
        default: 'root-module'
      backend_config:
        description: 'Backend configuration variables'
        required: false
        type: string
        default: ''
      terraform_variables:
        description: 'Terraform variables as JSON string'
        required: false
        type: string
        default: '{}'
      auto_approve:
        description: 'Auto approve terraform destroy'
        required: false
        type: boolean
        default: true
    secrets:
      DATADOG_API_KEY:
        required: true
      DATADOG_APP_KEY:
        required: true

jobs:
  terraform-destroy-apply:
    name: Terraform Destroy Apply
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ inputs.terraform_version }}

      - name: Terraform Init
        working-directory: ${{ inputs.working_directory }}
        run: |
          terraform init ${{ inputs.backend_config }}
        env:
          TF_VAR_datadog_api_key: ${{ secrets.DATADOG_API_KEY }}
          TF_VAR_datadog_app_key: ${{ secrets.DATADOG_APP_KEY }}

      - name: Download Terraform Destroy Plan
        uses: actions/download-artifact@v4
        with:
          name: terraform-destroy-plan
          path: ${{ inputs.working_directory }}

      - name: Terraform Destroy Apply
        working-directory: ${{ inputs.working_directory }}
        run: |
          if [ "${{ inputs.auto_approve }}" = "true" ]; then
            terraform apply -auto-approve tfplan-destroy
          else
            terraform apply tfplan-destroy
          fi
        env:
          TF_VAR_datadog_api_key: ${{ secrets.DATADOG_API_KEY }}
          TF_VAR_datadog_app_key: ${{ secrets.DATADOG_APP_KEY }}
